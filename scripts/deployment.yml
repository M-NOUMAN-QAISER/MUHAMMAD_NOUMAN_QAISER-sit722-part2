# Write Kubernetes Deployment Script
apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-catalog
spec:
  replicas: 2
  selector:
    matchLabels:
      app: book-catalog
  template:
    metadata:
      labels:
        app: book-catalog
    spec:
      containers:
      - name: book-catalog
        image: book_catalog_microservice:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://muhammad_nouman_qaiser_dcpd_user:feZH2MQpnHcdejg7c2LbLgSQh25XlDH4@dpg-crg8v288fa8c73an690g-a.oregon-postgres.render.com/muhammad_nouman_qaiser_dcpd"
---
apiVersion: v1
kind: Service
metadata:
  name: book-catalog-service
spec:
  selector:
    app: book-catalog
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: NodePort
